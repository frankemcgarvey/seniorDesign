/*
 * definition.h
 *
 *  Created on: Aug 13, 2021
 *      Author: Chris
 */

#ifndef COMMON_FOLDER_INC_DEFINITION_H_
#define COMMON_FOLDER_INC_DEFINITION_H_

#include "stm32h7xx_hal.h"
#include "arm_math.h"

#define PCM_CHUNK_SIZE 		1024
#define CHANNEL_NUMBER		2

#define SHIFT_AMOUNT		-8


#define TAPS 				301

typedef enum{
	NONE,
	HALF,
	FULL,
	READY,
}flag_t;

typedef struct{
	ALIGN_32BYTES(int32_t leftChannel[2*PCM_CHUNK_SIZE]);
	ALIGN_32BYTES(int32_t rightChannel[2*PCM_CHUNK_SIZE]);
	ALIGN_32BYTES(int32_t rightChannel[2*PCM_CHUNK_SIZE]);
	ALIGN_32BYTES(float pcmBuffer[2*CHANNEL_NUMBER][PCM_CHUNK_SIZE]);
}buffer_t;

typedef struct{
	arm_fir_instance_f32  Filter_inst;
	float state[TAPS + PCM_CHUNK_SIZE - 1];
}dsp_buffer_t;
#endif /* COMMON_FOLDER_INC_DEFINITION_H_ */
